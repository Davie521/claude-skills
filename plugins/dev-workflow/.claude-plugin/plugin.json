{
  "name": "dev-workflow",
  "version": "1.9.0",
  "description": "Dev workflow: Git PR pipeline, Copilot lint, multi-agent impl, browser MCP, git patterns, repo audit, multi-session blueprints, diagnosis loop, plan grilling (lite + docs-aware with CONTEXT.md/ADR), codebase deepening (deep modules / seams / deletion test), caveman compressed-comms mode, conversation-to-PRD synthesis, throwaway prototype (logic TUI / UI variants), issue triage state machine (with .out-of-scope knowledge base)",
  "author": {
    "name": "Yifan"
  }
}
